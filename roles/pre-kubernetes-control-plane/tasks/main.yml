- name: Pre control planes hooks
  ansible.builtin.include_tasks: "{{ item }}"
  with_items: "{{ kubernetes_hookfiles.pre_control_planes }}"
  when:
  - inventory_hostname in groups['control_planes']
  - kubernetes_hookfiles.pre_control_planes is defined
  run_once: true
