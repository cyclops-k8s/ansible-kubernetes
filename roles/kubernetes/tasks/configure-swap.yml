- name: Remove swapfile from /etc/fstab
  with_items:
  - swap
  - none
  ansible.posix.mount:
    fstype: swap
    path: "{{ item }}"
    state: absent

- name: Disable swap
  changed_when: true
  when: ansible_facts["swaptotal_mb"] > 0 and not kubernetes_allow_swap
  ansible.builtin.command: swapoff -a
