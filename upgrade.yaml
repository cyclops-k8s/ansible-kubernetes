- become: true
  gather_facts: true
  hosts: kubernetes
  name: Upgrade nodes
  serial: 1
  vars:
    version: "{{ kubernetes_version }}"
  roles:
  - role: kubernetes-defaults
  - role: kubernetes-upgrade
